// Ejemplo Módulos: Importación y Exportación

// Importar miembros específicos
import { PI, sumar, restar } from "./math_lib";

// Importar todo como un namespace (si el lenguaje lo soporta, o imports por defecto)
// import * as Math from "./math_lib"; 

echo("--- Usando Módulos ---");
echo("Valor de PI importado: " + PI);

let resultadoSuma = sumar(10, 20);
echo("Suma desde módulo: " + resultadoSuma);

let resultadoResta = restar(50, 20);
echo("Resta desde módulo: " + resultadoResta);