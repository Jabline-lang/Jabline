// Return Values - Functions returning results

fn square(x) {
    return x * x
}

fn addThenDouble(a, b) {
    let sum = a + b
    return sum * 2
}

let result = square(5)
print("5 squared = " + toString(result))

let doubled = addThenDouble(3, 4)
print("(3 + 4) * 2 = " + toString(doubled))
